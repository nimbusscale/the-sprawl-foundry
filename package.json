{
  "name": "the-sprawl",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node generate-packs.mjs",
    "build": "node build.mjs",
    "build:packs": "node build.mjs",
    "deploy": "bash deploy.sh"
  },
  "devDependencies": {
    "@foundryvtt/foundryvtt-cli": "latest"
  }
}
